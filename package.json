{
  "name": "@sushiswap/token-list-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "check": "tsc --pretty --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf .vercel",
    "lint": "TIMING=1 eslint --fix ."
  },
  "dependencies": {
    "@sushiswap/default-token-list": "^39.8.0",
    "@vercel/node": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^4.9.4",
    "vercel": "28.15.0"
  }
}
